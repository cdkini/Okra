// Finding the approximate square root of a number using the Babylonian method in Okra!


func sqrt : number, precision : {
    var s: number;
    for ((s - number / s) > precision) {
        s: (s + number / s) / 2;
    }
    return s;
}

print sqrt(5, 0.00001);